{
  "name": "aetherbot-library",
  "version": "1.0.0",
  "private": true,
  "description": "Solo practice repository for the AetherLink Worldline Wave 2 classroom (Teaching Days 1-2) — the AetherBot Library practice project.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "mcp": "node mcp-server.mjs",
    "validate": "node scripts/validate.js",
    "validate:fixtures": "node scripts/validate.js --fixtures",
    "reset": "git checkout -- ."
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
